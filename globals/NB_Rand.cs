using Godot;
using System;

public partial class NB_Rand : Node
{
	//TODO add global random variables ;-;
	// public string seed_string { get; set; }
	// // Called when the node enters the scene tree for the first time.

	// public RandomNumberGenerator getRandom()
	// {
	// 	RandomNumberGenerator randomizer = new RandomNumberGenerator();
	// 	randomizer.Seed = (ulong)String.GetHashCode(seed_string);
	// 	GD.Print("A".Sha256Text);
	// 	GD.Print(randomizer.RandiRange(0,5));
	// 	GD.Print(randomizer.RandiRange(0,5));
	// 	GD.Print(randomizer.RandiRange(0,5));
	// 	GD.Print(randomizer.RandiRange(0,5));
	// 	return null;		
	// }
}
